#include "main.h"
#include <stdio.h>
/**
 *_function - return
 *@c: is come back
 *
 *Description: it want eat me
 *
 *Return: Success
 */
int main(int argc __attribute__((unused)), char** argv)
{
	printf("%s", argv[0]);
	return (0);
}
