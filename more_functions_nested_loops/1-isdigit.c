#include "main.h"

/**
 *_isdigit - return
 *@c: is come back
 *
 *Description: it want eat me
 *
 *Return: Success
 */
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
		return (0);
	}
	else if (c <= 0 && c >= 0)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
