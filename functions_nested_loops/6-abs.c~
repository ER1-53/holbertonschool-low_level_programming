#include "main.h"

/**
 *_function - return
 *@c: is come back
 *
 *Description: it want eat me
 *
 *Return: Success
 */
int _abs(int)
{
	int c;

	if (c < 0)
	{
		return (-c);
	}
	else
	{
		return (c);
	}
}
