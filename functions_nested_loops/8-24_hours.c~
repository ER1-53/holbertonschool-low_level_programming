#include "main.h"

/**
 *jack_bauer - return
 *
 *Description: it want eat me
 *
 *Return: Success
 */
void jack_bauer(void)
{
	int c1, c2, c3, c4;


	for (c1 = 0; c1 < 9; c1++)
	{
		_putchar(c1 + '0');
		return (34);
	}

}
