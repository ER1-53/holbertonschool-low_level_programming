#include "main.h"
#include <stdio.h>

/**
 *create_array - return
 *@size: is come back
 *@c: the other
 *
 *Description: it want eat me
 *
 *Return: Success
 */
char *create_array(unsigned int size, char c)
{
	if (size == 0)
	return NULL;

	
}
